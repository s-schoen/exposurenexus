name: CI
permissions:
  contents: read

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run lint
        run: pnpm lint

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm build

  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run API unit tests
        run: pnpm --filter @exposurenexus/api test

      - name: Install Playwright Chromium
        run: pnpm --filter @exposurenexus/ui exec playwright install --with-deps chromium

      - name: Run UI tests
        run: pnpm --filter @exposurenexus/ui test

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run API coverage
        run: pnpm --filter @exposurenexus/api test:coverage

      - name: Install Playwright Chromium
        run: pnpm --filter @exposurenexus/ui exec playwright install --with-deps chromium

      - name: Run UI coverage
        run: pnpm --filter @exposurenexus/ui test:coverage

      - name: Upload API coverage
        uses: codecov/codecov-action@v5
        with:
          use_oidc: true
          disable_search: true
          files: apps/api/coverage/lcov.info
          flags: api
          fail_ci_if_error: false

      - name: Upload UI coverage
        uses: codecov/codecov-action@v5
        with:
          use_oidc: true
          disable_search: true
          files: apps/ui/coverage/lcov.info
          flags: ui
          fail_ci_if_error: false
